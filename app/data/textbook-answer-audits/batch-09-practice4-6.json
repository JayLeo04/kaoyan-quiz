{
  "schemaVersion": "textbook-answer-audit-v1",
  "bookId": "data-structures-yan-weimin",
  "scope": [
    "practice-4",
    "practice-5",
    "practice-6"
  ],
  "updates": [
    {
      "id": "book-ds-yan-practice-4-4-1",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n采用行、列均从 1 开始的带行逻辑链接三元组顺序表。输入三元组必须按行优先严格递增、坐标在矩阵范围内且值非零；rpos[i] 是第 i 行首项，rpos[mu+1]=tu+1。\n\n~~~~c\n#define MAXRC 20\n#define MAXTERM (MAXRC*MAXRC)\ntypedef struct { int i,j,e; } Triple;\ntypedef struct { Triple data[MAXTERM+1]; int mu,nu,tu,rpos[MAXRC+2]; } RLSMatrix;\nvoid BuildRpos(RLSMatrix *M) {\n  int p=1;\n  for (int i=1;i<=M->mu;++i) { M->rpos[i]=p; while (p<=M->tu && M->data[p].i==i) ++p; }\n  M->rpos[M->mu+1]=M->tu+1;\n}\n~~~~\n\n加、减法先检查同型，然后归并两个已排序三元组表：较小坐标直接写入 C；相同坐标写入 A.e 加 sign 乘 B.e，结果为零则省略。C 必须重新生成而不能修改 A 或 B。乘法先检查 A.nu 等于 B.mu；逐行处理 A，令长度 B.nu 的临时数组 temp 清零。对 A 行中的每项 (i,k,a)，只枚举 B 的第 k 行并做 temp[j] 加等于 a 乘 B[k,j]，最后把 temp 的非零项按 j 递增写入 C。每次写入检查 MAXTERM，乘加使用足够宽的整数并检查溢出。\n\n~~~~text\nAddSub: p=q=1，C.tu=0；按 (i,j) 归并，必要时 Append(C,i,j,value)；BuildRpos(C)。\nMultiply: 对 i=1..A.mu：temp[1..B.nu]=0；\n          对 p=A.rpos[i]..A.rpos[i+1)-1：\n             k=A.data[p].j；对 q=B.rpos[k]..B.rpos[k+1)-1：\n                 temp[B.data[q].j]+=A.data[p].e*B.data[q].e；\n          把本行 temp 的非零项 Append 到 C；最后 BuildRpos(C)。\n~~~~\n\n归并不变式说明 C 的已写前缀正是已处理坐标的和或差；内层累积恰为每个 C[i,j] 的点积。加减时间 O(tuA+tuB)、额外工作空间 O(1)；乘法时间为 O(A.mu*B.nu 加上 A 的每个非零项所对应的 B 行非零数之和)，临时空间 O(B.nu)，结果空间 O(tuC)。\n\n题列第三个乘法样例左上项应为 4 乘 3 加 (-3) 乘 (-4) 加 1 乘 0，结果 24。因此独立核验的第一行为 [24,-6,0]，并非题面印出的 [0,-6,0]；其余三行是 [8,0,0]、[0,1,0]、[0,0,0]。这只说明核验结果，不改写原题数据。",
        "explanation": "严格使用带行逻辑链接的三元组顺序表，覆盖维度、容量、零项、复杂度和乘法样例不一致。",
        "correction": {
          "verified": "**完整参考实现（非原书摘录）**\n\n约定：行、列从 1 开始，输入三元组按 (i,j) 严格递增、坐标合法且元素非零。AddSub 的 sign 为 1 或 -1，分别计算和、差；Multiply 计算积。维数不匹配、容量不足或算术溢出时返回错误且不改写输出。rpos[i] 是第 i 行首项，rpos[mu+1]=tu+1。\n\n~~~~c\n#include <limits.h>\n#define MAXRC 20\n#define MAXTERM (MAXRC*MAXRC)\ntypedef long long E; typedef enum { OK,DIM_ERROR,CAPACITY_ERROR,ARITH_ERROR } Status;\ntypedef struct {int i,j;E e;} Triple;\ntypedef struct {Triple data[MAXTERM+1];int mu,nu,tu,rpos[MAXRC+2];} RLS;\nstatic void Rpos(RLS*m){int p=1;for(int i=1;i<=m->mu;i++){m->rpos[i]=p;while(p<=m->tu&&m->data[p].i==i)p++;}m->rpos[m->mu+1]=m->tu+1;}\nstatic Status Put(RLS*m,int i,int j,E e){if(!e)return OK;if(m->tu==MAXTERM)return CAPACITY_ERROR;m->data[++m->tu]=(Triple){i,j,e};return OK;}\nstatic int Before(Triple x,Triple y){return x.i<y.i||(x.i==y.i&&x.j<y.j);} static int Fits(__int128 x){return x>=LLONG_MIN&&x<=LLONG_MAX;}\nStatus AddSub(const RLS*a,const RLS*b,int sign,RLS*out){\n if(!a||!b||!out||(sign!=1&&sign!=-1)||a->mu!=b->mu||a->nu!=b->nu)return DIM_ERROR;RLS c={.mu=a->mu,.nu=a->nu};int p=1,q=1;\n while(p<=a->tu||q<=b->tu){Status s;if(q>b->tu||(p<=a->tu&&Before(a->data[p],b->data[q])))s=Put(&c,a->data[p].i,a->data[p].j,a->data[p++].e);else if(p>a->tu||Before(b->data[q],a->data[p])){__int128 v=(__int128)sign*b->data[q].e;if(!Fits(v))return ARITH_ERROR;s=Put(&c,b->data[q].i,b->data[q].j,(E)v);q++;}else{__int128 v=(__int128)a->data[p].e+(__int128)sign*b->data[q].e;if(!Fits(v))return ARITH_ERROR;s=Put(&c,a->data[p].i,a->data[p].j,(E)v);p++;q++;}if(s!=OK)return s;}Rpos(&c);*out=c;return OK;\n}\nStatus Multiply(const RLS*a,const RLS*b,RLS*out){\n if(!a||!b||!out||a->nu!=b->mu)return DIM_ERROR;RLS c={.mu=a->mu,.nu=b->nu};for(int i=1;i<=a->mu;i++){__int128 sum[MAXRC+1]={0};for(int p=a->rpos[i];p<a->rpos[i+1];p++){int k=a->data[p].j;for(int q=b->rpos[k];q<b->rpos[k+1];q++)sum[b->data[q].j]+=(__int128)a->data[p].e*b->data[q].e;}for(int j=1;j<=b->nu;j++){if(!Fits(sum[j]))return ARITH_ERROR;Status s=Put(&c,i,j,(E)sum[j]);if(s!=OK)return s;}}Rpos(&c);*out=c;return OK;\n}\n~~~~\n\n可复验样例：A 为 (1,1,10),(2,3,9),(3,1,-1)，B 为 (2,3,-1),(3,1,1),(3,3,-3)，分别建 rpos 后相加得到 [[10,0,0],[0,0,8],[0,0,-3]]。乘法题例左上角为 4×3+(-3)×(-4)+1×0=24，程序第一行是 [24,-6,0]；题面 [0,-6,0] 与其数据不一致。\n\n归并不变式为 C 已写前缀恰是已处理坐标的和或差；内层累积恰为点积。加减时间 O(tuA+tuB)、空间 O(1)；乘法时间 O(muA·nuB+Σ使用的B行非零数)，工作空间 O(nuB)。",
          "explanation": "保留行逻辑链接三元组和乘法样例核验，补为完整可调用的加减乘实现。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [
          "PRACTICE_4_ANSWER_MISSING"
        ],
        "notes": "已补入独立算法并明确标注可核验的样例不一致。"
      }
    },
    {
      "id": "book-ds-yan-practice-4-4-2",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n用行优先映像实现不超过 MaxDim 维的整型数组。创建时读入每维 lower[d]、upper[d]，要求 lower[d] 不大于 upper[d]；len[d]=upper[d]-lower[d]+1。常量表从后向前计算：constants[dim-1]=1，constants[d]=constants[d+1] 乘 len[d+1]；总元素数为所有 len 的乘积，乘法前检查溢出。\n\n~~~~c\nStatus Locate(const NArray *a, const int sub[], size_t *off) {\n  size_t x=0;\n  for (int d=0; d<a->dim; ++d) {\n    if (sub[d]<a->lower[d] || sub[d]>a->upper[d]) return ERROR;\n    x += (size_t)(sub[d]-a->lower[d])*a->constants[d];\n  }\n  *off=x; return OK;\n}\nStatus InitNArray(NArray *a,int dim,const int lo[],const int hi[]) {\n  if (dim<1 || dim>MaxDim) return ERROR;\n  a->dim=dim; a->num=1;\n  for (int d=0; d<dim; ++d) {\n    if (lo[d]>hi[d]) return ERROR;\n    a->lower[d]=lo[d]; a->upper[d]=hi[d];\n    size_t len=(size_t)(hi[d]-lo[d]+1);\n    if (a->num>SIZE_MAX/len) return OVERFLOW;\n    a->num*=len;\n  }\n  a->constants[dim-1]=1;\n  for (int d=dim-2; d>=0; --d) a->constants[d]=a->constants[d+1]*(a->upper[d+1]-a->lower[d+1]+1);\n  a->elem=calloc(a->num,sizeof *a->elem); return a->elem?OK:OVERFLOW;\n}\n~~~~\n\nGet、Set 先 Locate 再访问 elem[off]，所以越界不会解引用。完整数组赋值要求维数及每维上下界都相同，随后复制 num 项。子数组赋值接收源、目的及每维闭区间；先检查各区间在界内且对应长度相等，令 P 为元素数。对 t=0 到 P-1 用混合进制解码得到各维相对坐标并分别 Locate；必须先把 P 个右值读入临时缓冲，再按同一顺序写入目的。这样一维前移、后移以及任意多维重叠矩形赋值都等价于先求完整右值后赋左值。\n\nLocate 是合法下标笛卡儿积到 0 到 num-1 的一一映射，临时缓冲保留了重叠赋值前的右值。初始化 O(dim)，单元素访问 O(dim)，完整赋值 O(num)，子数组赋值 O(P*dim) 时间和 O(P) 临时空间；数组大小是 num，元素存储为 num 乘 ElemType 大小。",
        "explanation": "给出任意起始下标的映像、范围检查、同类型赋值以及重叠安全的子数组赋值。",
        "correction": {
          "verified": "**完整参考实现（非原书摘录）**\n\n约定：严格按题设分离 NArray 类型记录和 NArrayType 对象；行优先，维数 1..MaxDim，各维闭区间可从任意 int 起始。初始化对象须清零，失败不改变它；读写下标长度为维数，完整赋值要求各维上下界相同。\n\n~~~~c\n#include <stdint.h>\n#include <stddef.h>\n#include <stdlib.h>\n#include <string.h>\n#include <limits.h>\n#define MaxDim 5\ntypedef int ElemType; typedef enum {OK,ERROR,OVERFLOW,NO_MEMORY,TYPE_MISMATCH} Status;\ntypedef struct {int dim;int*lower,*upper;size_t*constants;} NArray,*NArrayPtr;\ntypedef struct {ElemType*elem;int num;NArrayPtr TypeRecord;} NArrayType;\nvoid DestroyNArray(NArrayType*a){if(!a)return;free(a->elem);if(a->TypeRecord){free(a->TypeRecord->lower);free(a->TypeRecord->upper);free(a->TypeRecord->constants);free(a->TypeRecord);}*a=(NArrayType){0};}\nStatus Locate(const NArrayType*a,const int sub[],size_t*off){const NArray*r=a?a->TypeRecord:NULL;if(!r||!sub||!off)return ERROR;size_t k=0;for(int d=0;d<r->dim;d++){if(sub[d]<r->lower[d]||sub[d]>r->upper[d])return ERROR;k+=(size_t)((int64_t)sub[d]-r->lower[d])*r->constants[d];}if(k>=(size_t)a->num)return ERROR;*off=k;return OK;}\nStatus InitNArray(NArrayType*a,int dim,const int lo[],const int hi[]){\n if(!a||a->elem||a->TypeRecord||a->num||!lo||!hi||dim<1||dim>MaxDim)return ERROR;NArray*r=calloc(1,sizeof*r);if(!r)return NO_MEMORY;r->lower=malloc(dim*sizeof*r->lower);r->upper=malloc(dim*sizeof*r->upper);r->constants=malloc(dim*sizeof*r->constants);if(!r->lower||!r->upper||!r->constants){free(r->lower);free(r->upper);free(r->constants);free(r);return NO_MEMORY;}size_t n=1,w[MaxDim];r->dim=dim;\n for(int d=0;d<dim;d++){int64_t z=(int64_t)hi[d]-lo[d]+1;if(z<=0||(uint64_t)z>SIZE_MAX||n>SIZE_MAX/(size_t)z||n>(size_t)INT_MAX/(size_t)z){free(r->lower);free(r->upper);free(r->constants);free(r);return OVERFLOW;}r->lower[d]=lo[d];r->upper[d]=hi[d];w[d]=(size_t)z;n*=w[d];}r->constants[dim-1]=1;for(int d=dim-2;d>=0;d--){if(r->constants[d+1]>SIZE_MAX/w[d+1]){free(r->lower);free(r->upper);free(r->constants);free(r);return OVERFLOW;}r->constants[d]=r->constants[d+1]*w[d+1];}ElemType*e=calloc(n,sizeof*e);if(!e){free(r->lower);free(r->upper);free(r->constants);free(r);return NO_MEMORY;}a->elem=e;a->num=(int)n;a->TypeRecord=r;return OK;\n}\nStatus Get(const NArrayType*a,const int s[],ElemType*v){size_t k;if(!v||Locate(a,s,&k)!=OK)return ERROR;*v=a->elem[k];return OK;} Status Set(NArrayType*a,const int s[],ElemType v){size_t k;if(Locate(a,s,&k)!=OK)return ERROR;a->elem[k]=v;return OK;}\nstatic int Same(const NArrayType*a,const NArrayType*b){if(!a||!b||!a->TypeRecord||!b->TypeRecord||a->num!=b->num||a->TypeRecord->dim!=b->TypeRecord->dim)return 0;for(int d=0;d<a->TypeRecord->dim;d++)if(a->TypeRecord->lower[d]!=b->TypeRecord->lower[d]||a->TypeRecord->upper[d]!=b->TypeRecord->upper[d])return 0;return 1;}\nStatus Assign(NArrayType*d,const NArrayType*s){if(!Same(d,s))return TYPE_MISMATCH;memmove(d->elem,s->elem,(size_t)s->num*sizeof*s->elem);return OK;}\nStatus SubAssign(NArrayType*d,const int dl[],const int dh[],const NArrayType*s,const int sl[],const int sh[]){\n if(!d||!s||!d->TypeRecord||!s->TypeRecord||!dl||!dh||!sl||!sh||d->TypeRecord->dim!=s->TypeRecord->dim)return TYPE_MISMATCH;int dim=s->TypeRecord->dim,rel[MaxDim],si[MaxDim],di[MaxDim];size_t len[MaxDim],n=1;for(int k=0;k<dim;k++){if(sl[k]<s->TypeRecord->lower[k]||sh[k]>s->TypeRecord->upper[k]||dl[k]<d->TypeRecord->lower[k]||dh[k]>d->TypeRecord->upper[k])return ERROR;int64_t x=(int64_t)sh[k]-sl[k]+1,y=(int64_t)dh[k]-dl[k]+1;if(x<=0||x!=y||(uint64_t)x>SIZE_MAX||n>SIZE_MAX/(size_t)x)return ERROR;len[k]=(size_t)x;n*=len[k];}if(n>SIZE_MAX/sizeof(ElemType))return OVERFLOW;ElemType*tmp=malloc(n*sizeof*tmp);if(!tmp)return NO_MEMORY;\n for(size_t p=0;p<n;p++){size_t x=p,o;for(int k=dim-1;k>=0;k--){rel[k]=x%len[k];x/=len[k];si[k]=sl[k]+rel[k];}if(Locate(s,si,&o)!=OK){free(tmp);return ERROR;}tmp[p]=s->elem[o];}for(size_t p=0;p<n;p++){size_t x=p,o;for(int k=dim-1;k>=0;k--){rel[k]=x%len[k];x/=len[k];di[k]=dl[k]+rel[k];}if(Locate(d,di,&o)!=OK){free(tmp);return ERROR;}d->elem[o]=tmp[p];}free(tmp);return OK;\n}\n~~~~\n\n可复验样例：用下界 {1,3}、上界 {3,5} 建 3×3 数组，令 a[i,j]=10*i+j。执行 SubAssign 的目标 {1,3}..{3,4}、源 {1,4}..{3,5} 后，每行 [10i+3,10i+4,10i+5] 变为 [10i+4,10i+5,10i+5]。越界、维数不同、长度不同均返回错误且不写入；临时缓冲保证前后移和多维重叠安全。\n\nconstants[d] 是第 d 维加一跳过的后缀元素数。初始化 O(dim)，读写 O(dim)，完整赋值 O(num)，P 元素子数组赋值 O(P·dim) 时间、O(P+dim) 空间。",
          "explanation": "吸收既有类型分离修正，补足三张表的分配释放、映像、读写、同型与重叠安全子数组赋值。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。；已吸收上一版修正：原补充答案将 num、elem 错放在 NArray 上，且未分配 lower、upper、constants 三张指针表，无法按题设定义直接实现。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [
          "PRACTICE_4_ANSWER_MISSING"
        ],
        "notes": "已按题设多维数组和连续区间补入可执行设计。"
      }
    },
    {
      "id": "book-ds-yan-practice-4-4-3",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n采用头尾链表结点：空表用 NULL；原子结点保存完整字母数字串；表结点保存 hp（第一项）和 tp（去掉第一项后的表）。输入可跨行，逐字符滤掉空白并计数括号；计数不能为负，最外层括号闭合才结束，最终不平衡或原子含非字母数字均报告语法错误。\n\n~~~~c\ntypedef enum { ATOM, LIST } Tag;\ntypedef struct GLNode {\n  Tag tag;\n  union { char *atom; struct { struct GLNode *hp,*tp; } ptr; } u;\n} GLNode,*GList;\n~~~~\n\n对无最外层括号的逗号序列 s，test(s) 分别在空、纯原子、其他时返回 N、E、O。hsub 从左扫描并维护括号深度，在深度零的首个逗号处截取前缀；tsub 取该逗号后缀；strip 仅在首尾是一对覆盖整个串的括号时去掉它们。ParseExpr 对空括号返回 NULL、对原子建 ATOM、对其他括号式调用 ParseSeq(strip(s))；ParseSeq 在空 s 返回 NULL，否则新建 LIST，令 hp=ParseExpr(hsub(s))、tp=ParseSeq(tsub(s))。实现用原串的起止下标；实际建表时让一次 NextItem 扫描同时产出 hsub 和 tsub 的两个边界，再递归解析该项，避免在深层嵌套中反复扫描同一片段。\n\n执行操作串时，h 或 t 都要求当前对象为非空 LIST；h 令当前对象为 hp，t 令其为 tp，其他字符或对原子、空表取头尾都报错。递归 Print 原样输出原子，并从 hp、tp 重构括号和逗号。题给 ((),(e1),(abc,(e2,c,dd))) 经 tth 后为 (abc,(e2,c,dd))。\n\n解析递归与头尾分解逐层对应，h/t 也正是广义表定义的头/尾。使用上述共享边界扫描时，设过滤后长度 L、操作串长度 q，建立时间和结构空间 O(L)，操作 O(q)，打印 O(结果长度)，递归深度最坏为嵌套深度。",
        "explanation": "严格使用头尾分解的广义表结构，覆盖多行输入、语法、非法操作与给定测试结果。",
        "correction": {
          "verified": "**完整参考实现（非原书摘录）**\n\n约定：NULL 是空表；ATOM 保存完整字母数字串；LIST.hp 是首项，LIST.tp 是余项。语法为 List := '(' [Item {',' Item}] ')'、Item := List 或 alnum+；跨行空白被忽略。非法原子、连续逗号、空项、未配对括号及尾随字符都失败并释放结点。解析只前向读取一次，不先用 hsub/tsub 扫描嵌套边界。\n\n~~~~c\n#include <ctype.h>\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\ntypedef enum {ATOM,LIST} Tag; typedef struct N{Tag tag;union{char*atom;struct{struct N*hp,*tp;}p;}u;}N,*GList; typedef struct{const char*s;size_t pos,n;int ok;}P;\nstatic void Free(GList x){if(!x)return;if(x->tag==ATOM)free(x->u.atom);else{Free(x->u.p.hp);Free(x->u.p.tp);}free(x);} static void Skip(P*p){while(p->pos<p->n&&isspace((unsigned char)p->s[p->pos]))p->pos++;} static GList Item(P*p);\nstatic GList List(P*p){if(p->pos>=p->n||p->s[p->pos++]!='('){p->ok=0;return NULL;}Skip(p);if(p->pos<p->n&&p->s[p->pos]==')'){p->pos++;return NULL;}GList h=NULL,*tail=&h;for(;;){GList x=Item(p);if(!p->ok){Free(h);return NULL;}GList c=malloc(sizeof*c);if(!c){Free(x);Free(h);p->ok=0;return NULL;}c->tag=LIST;c->u.p.hp=x;c->u.p.tp=NULL;*tail=c;tail=&c->u.p.tp;Skip(p);if(p->pos>=p->n){Free(h);p->ok=0;return NULL;}if(p->s[p->pos]==')'){p->pos++;return h;}if(p->s[p->pos++]!=','){Free(h);p->ok=0;return NULL;}Skip(p);if(p->pos>=p->n||p->s[p->pos]==','||p->s[p->pos]==')'){Free(h);p->ok=0;return NULL;}}}\nstatic GList Item(P*p){Skip(p);if(p->pos<p->n&&p->s[p->pos]=='(')return List(p);size_t b=p->pos;while(p->pos<p->n&&isalnum((unsigned char)p->s[p->pos]))p->pos++;if(b==p->pos){p->ok=0;return NULL;}GList x=malloc(sizeof*x);char*w=malloc(p->pos-b+1);if(!x||!w){free(x);free(w);p->ok=0;return NULL;}memcpy(w,p->s+b,p->pos-b);w[p->pos-b]=0;x->tag=ATOM;x->u.atom=w;return x;}\nGList Create(const char*s,int*ok){P p={s,0,s?strlen(s):0,s!=NULL};Skip(&p);GList x=p.ok?List(&p):NULL;Skip(&p);if(!p.ok||p.pos!=p.n){Free(x);x=NULL;p.ok=0;}if(ok)*ok=p.ok;return x;} void Print(GList x){if(!x){putchar('(');putchar(')');return;}if(x->tag==ATOM){for(char*p=x->u.atom;*p;p++)putchar(*p);return;}putchar('(');for(GList p=x;p;p=p->u.p.tp){Print(p->u.p.hp);if(p->u.p.tp)putchar(',');}putchar(')');}\nint Ops(GList root,const char*ops,GList*out){GList x=root;for(size_t i=0;ops&&ops[i];i++){if(!x||x->tag!=LIST||(ops[i]!='h'&&ops[i]!='t'))return 0;x=ops[i]=='h'?x->u.p.hp:x->u.p.tp;}*out=x;return 1;}\n~~~~\n\n字符串演示函数仍可按题意实现：test 返 N/E/O；hsub 在深度零的首逗号前取首项；tsub 取逗号后缀；strip 仅剥去覆盖全串的首尾括号。\n\n可复验样例：输入 ((),(e1),(abc,(e2,c,dd)))，Ops(root,'tth',&r) 成功，Print(r) 输出 (abc,(e2,c,dd))；输入 (a,,b) 建表失败；当前值为空表或原子后继续 h/t 失败。r 与 root 共享结点，只能最终 Free(root) 一次。\n\n结构归纳表明每项恰接为一个头尾单元。长度 L 的建表时间和存储 O(L)，递归栈 O(嵌套深度)，q 个操作 O(q)，打印 O(结果长度)。",
          "explanation": "吸收单前向游标解析修正，给出完整 hp/tp 建表、析构、打印、操作错误与样例。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。；已吸收上一版修正：原补充答案先扫描嵌套首项边界再递归解析该首项，深度单元素嵌套时会重复跨越同一字符，最坏不是所称的 O(L)。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [
          "PRACTICE_4_ANSWER_MISSING"
        ],
        "notes": "已补入广义表建立、头尾操作和输出的独立解答。"
      }
    },
    {
      "id": "book-ds-yan-practice-4-4-4",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n基本题文法为 Expr := digit 或 ( + Expr Expr)，digit 仅为 0 到 9，空白可出现在任意记号之间。递归下降时先跳过空白：读到数字即返回；读到左括号后必须依次读到加号、两个 Expr 和右括号；任一缺失、额外右括号或行尾尚有非空白字符均为语法错误。\n\n~~~~c\nStatus ParseExpr(const char **p,long long *v) {\n  SkipSpace(p);\n  if (**p>='0' && **p<='9') { *v=**p-'0'; ++*p; return OK; }\n  if (**p!='(') return ERROR;\n  ++*p; SkipSpace(p); if (**p!='+') return ERROR; ++*p;\n  long long a,b;\n  if (ParseExpr(p,&a)!=OK || ParseExpr(p,&b)!=OK) return ERROR;\n  SkipSpace(p);\n  if (**p!=')' || AddWouldOverflow(a,b)) return ERROR;\n  ++*p; *v=a+b; return OK;\n}\nStatus EvaluateLine(const char *line,int *ans) {\n  const char *p=line; long long v;\n  if (ParseExpr(&p,&v)!=OK) return ERROR;\n  SkipSpace(&p);\n  if (*p!='\\0' || v<INT_MIN || v>INT_MAX) return ERROR;\n  *ans=(int)v; return OK;\n}\n~~~~\n\nEvaluate(FILE 指针) 应逐行读入动态缓冲，调用 EvaluateLine；成功返回值，失败通过状态或错误标志报告行号，不能把错误当作 0。测试式的值依次为 6、9、15、15、36。数字基例正确；归纳地，括号式只在两个子表达式正确后相加，恰符合文法语义。每字符常数次处理，时间 O(L)，递归栈 O(depth)，最坏 O(L)，并保留加法溢出检查。多位整数或四则运算是选做扩展，必须另行修改词法和文法。",
        "explanation": "给出单数字加法文法一致的递归求值、整行校验、语法错误和整数溢出处理。",
        "correction": {
          "verified": "**完整参考实现（非原书摘录）**\n\n约定：基本文法为 Expr := digit 或 '(' '+' Expr Expr ')'；digit 仅 0..9，空白可在任意记号之间。每行只允许一个表达式；空行、缺符号/操作数/右括号、尾随字符和超出 int 的结果为错误。以 Status Evaluate(FILE*,int*) 替代无法报告错误的单一 int 接口，成功才写 out。\n\n~~~~c\n#include <ctype.h>\n#include <limits.h>\n#include <stdio.h>\n#include <stdlib.h>\ntypedef enum {OK,SYNTAX_ERROR,RANGE_ERROR,IO_ERROR,NO_MEMORY} Status;\nstatic void Skip(const char**p){while(isspace((unsigned char)**p))++*p;} static int Add(long long a,long long b,long long*s){if((b>0&&a>LLONG_MAX-b)||(b<0&&a<LLONG_MIN-b))return 0;*s=a+b;return 1;}\nstatic Status Expr(const char**p,long long*v){Skip(p);if(**p>='0'&&**p<='9'){*v=*(*p)++-'0';return OK;}if(**p!='(')return SYNTAX_ERROR;++*p;Skip(p);if(**p!='+')return SYNTAX_ERROR;++*p;long long a,b;Status s=Expr(p,&a);if(s!=OK||(s=Expr(p,&b))!=OK)return s;Skip(p);if(**p!=')')return SYNTAX_ERROR;++*p;return Add(a,b,v)?OK:RANGE_ERROR;}\nStatus EvaluateLine(const char*line,int*out){if(!line||!out)return IO_ERROR;const char*p=line;long long v;Status s=Expr(&p,&v);if(s!=OK)return s;Skip(&p);if(*p!=0)return SYNTAX_ERROR;if(v<INT_MIN||v>INT_MAX)return RANGE_ERROR;*out=(int)v;return OK;}\nstatic Status ReadLine(FILE*f,char**out){size_t n=0,c=64;int ch;char*s=malloc(c);if(!s)return NO_MEMORY;while((ch=fgetc(f))!=EOF&&ch!=10){if(n+1==c){char*t=realloc(s,c*2);if(!t){free(s);return NO_MEMORY;}s=t;c*=2;}s[n++]=ch;}if(ch==EOF&&n==0){free(s);return IO_ERROR;}s[n]=0;*out=s;return OK;}\nStatus Evaluate(FILE*f,int*out){char*s;Status r=ReadLine(f,&s);if(r!=OK)return r;r=EvaluateLine(s,out);free(s);return r;}\n~~~~\n\n可复验样例：题给五行依次输出 6、9、15、15、36。表达式 (+4)、(+4x)、(+4 5 和 (+4 5)) 都返回 SYNTAX_ERROR。相加先在 long long 检查溢出，再检查能否存入 int。\n\n数字基例直接正确；括号式仅在两个子式成功且右括号匹配后求和，故结构归纳正确。每字符常数次处理，时间 O(L)，栈 O(depth)。多位整数或四则运算必须另改词法和文法。",
          "explanation": "保留单数字加法文法、整行校验和溢出处理，补齐逐行输入、状态接口、递归核心和错误样例。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [
          "PRACTICE_4_ANSWER_MISSING"
        ],
        "notes": "已按题给 Evaluate 接口补入安全的递归求值设计。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-1",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n先删除空白并按优先级建立语法树，而不是按字符直接求值。递归下降文法可写为 Or := And { | And }，And := Not { & Not }，Not := ～ Not 或 Primary，Primary := A 到 Z 或 ( Or )；可额外接受 ASCII 波浪号作为非号。解析时记录变量的首次出现顺序；变量数超过 20、括号不配对或仍有残余字符均拒绝。\n\n对 k 个变量枚举 mask=0 到 2^k-1。mask 第 i 位给第 i 个变量赋 0/1，随后按后序递归求 AST：变量查值，非结点取反，与结点取逻辑与，或结点取逻辑或。维护 allTrue、allFalse 和首个真赋值；枚举结束后，allTrue 输出 True forever，allFalse 输出 False forever，否则输出 Satisfiable、变量名序列并进入交互赋值。交互输入必须恰有 k 个 0/1 值，再复用同一 AST 求值。\n\n题给六式的分类依次为：重言式、矛盾式、重言式、矛盾式、可满足但非重言式、可满足但非重言式。第五式等价于 A；第六式为异或，按 A、B 的 00、01、10、11 取值结果为 0、1、1、0。\n\n对 AST 的结构归纳说明 Eval 与逻辑运算的语义一致；枚举覆盖全部 2^k 个赋值，故三类结论完整且互斥。设表达式长度 L，建树 O(L) 时间和空间；分类 O(2^k*L) 时间，递归栈 O(L)，k 不超过 20 时最多枚举 1,048,576 组。",
        "explanation": "覆盖题给优先级、括号、变量上限、真值表穷举、分类和交互赋值。",
        "correction": {
          "verified": "## 解法\n\n输入一整行表达式（允许空格；`～`和`~`均表示非），变量限 `A`–`Z` 且不同变量不超过 20。先删除空白并按 `～ > & > |` 建立语法树，再枚举所有赋值，而不是边读字符边求值。采用递归下降文法：\n\n~~~text\nOr      := And { | And }\nAnd     := Not { & Not }\nNot     := ( ～ | ~ ) Not | Primary\nPrimary := A..Z | ( Or )\n~~~\n\n`parseOr`、`parseAnd` 用循环构造左结合二叉结点；`parseNot` 构造一元结点。每次读到变量即按首次出现顺序加入 `vars[]`。解析结束必须恰好到字符串末尾，否则报“非法字符、缺右括号或缺操作数”。\n\n~~~text\nEval(x, value):\n  if x.kind = VAR: return value[x.name]\n  if x.kind = NOT: return not Eval(x.left, value)\n  if x.kind = AND: return Eval(x.left, value) and Eval(x.right, value)\n  return Eval(x.left, value) or Eval(x.right, value)\n\nClassify(root, vars[0..k-1]):\n  allTrue = true; allFalse = true; firstTrue = none\n  for mask = 0 .. (1<<k)-1:\n    for i = 0 .. k-1: value[vars[i]] = (mask >> i) & 1\n    r = Eval(root, value)\n    allTrue = allTrue and r; allFalse = allFalse and (not r)\n    if r and firstTrue is none: firstTrue = copy(value)\n  if allTrue: print True forever\n  else if allFalse: print False forever\n  else: print Satisfiable, vars, firstTrue; InteractiveEval(root, vars)\n~~~\n\n交互阶段每次读取恰好 `k` 个以空格或逗号分隔的 `0/1`；数量不符或出现其他值就提示重输，输入 `q` 返回菜单。输出结果为同一棵树的 `Eval` 值。AST 结点在结束时后序释放。\n\n**可复验样例**：输入 `A&～B|～A&B`，变量顺序为 `A B`。四行输入 `0,0`、`0,1`、`1,0`、`1,1` 分别得到 `0,1,1,0`，因此输出 `Satisfiable`。`(A|～A)&(B|～B)` 输出 `True forever`；`(A&～A)&C` 输出 `False forever`。\n\n**正确性与复杂度**：归纳 AST 结构可知 `Eval` 等于子表达式的逻辑值；枚举覆盖全部 `2^k` 组取值，三类结论完整且互斥。设表达式长度为 `L`，建树时间、空间均为 `O(L)`，判别时间为 `O(2^k L)`，递归栈为 `O(L)`。",
          "explanation": "给出完整的解析、真值表枚举和交互求值流程，并保留原审校的优先级、变量上限和六个测试式结论。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入逻辑表达式建树、完整枚举和分类的独立解答。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-2",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n初始化 I 读取 n 个互异字符及正权值，n 小于 2、字符重复或权值非正均报错；用最小堆反复取出两个最小权结点，合成新父结点，直到只余根。相同权值以字符序号和创建序号打破平局，保证同一输入可复现。深度优先时左边记 0、右边记 1，得到字符到码字的表；若 n 为 1，约定唯一字符的码为单个 0。\n\nhfmTree 不能只保存码字，应以前序拓扑格式保存：内部结点记 I，叶结点记 L 加字符字节值，读入时递归重建；这能无歧义保存任意字符。E 在内存无树时先读取 hfmTree，再逐字符查码表，把位流紧凑写入 CodeFile，并在文件头记录有效位数或最后字节有效位数。D 沿 0/1 走左右孩子，到叶就输出字符并回根；读完时若不在根、走到空孩子或有效位数不符即报告损坏。P 将同一位流每行恰印 50 位并写入 CodePrin；T 以树形或凹入表显示并写入 TreePrint。菜单只接受 I、E、D、P、T、Q；一次会话中首次成功加载后缓存树，文件错误不破坏已有树。\n\n用题给频度表建立树后，对 THIS PROGRAM IS MY FAVORITE 编码再译码，输出必须逐字节等于原报文；具体位串会随并列权值的确定规则变化，不能脱离该规则声称唯一。哈夫曼贪心合并满足最优前缀码的交换论证；译码每次到叶输出并复位，故恰恢复一个码字序列。建树 O(n log n)，码表 O(n*h)，报文编码 O(总码长)、译码 O(位数)，树与码表空间 O(n)。",
        "explanation": "明确可复现建树、可逆文件格式、位流边界、单符号特例与编译码回归验证。",
        "correction": {
          "verified": "## 解法\n\n下面给出单文件核心的伪代码；文件均为二进制文件，避免把任意字符集误当作文本。菜单只接受 `I/E/D/P/T/Q`，除 `I` 外的命令在内存无树时先调用 `LoadTree(hfmTree)`；载入失败不覆盖已缓存的有效树。\n\n~~~text\nNode = {weight, ch, left, right, serial}       // 叶：left=right=nil\nBuild(items):\n  reject n < 1, 重复字符, weight <= 0\n  heap = items 按 (weight, 叶字符值, serial) 的最小堆\n  if n = 1: return heap.pop()\n  while heap.size > 1:\n    a = heap.pop(); b = heap.pop()\n    heap.push(Node(a.weight+b.weight, nil, a, b, nextSerial()))\n  return heap.pop()\nCodes(x, bits):\n  if x is leaf: code[x.ch] = (bits = empty ? 0 : bits); return\n  Codes(x.left, bits+0); Codes(x.right, bits+1)\n~~~\n\n**I（初始化）**：读 `n`、`(字符字节,正权值)` 对，调用 `Build`，调用 `Codes`，再把树写入 `hfmTree`。树文件格式为 `HFM1 + version + preorder(root)`；前序中内部结点写标记 `I`，叶写 `L` 和一个字符字节。读取时递归重建，验证恰有一棵树、叶字符不重复且没有多余字节。特别地，`n=1` 合法，唯一字符的码固定为 `0`。\n\n**E（编码）**：对输入文件 `ToBeTran` 的每个字节查 `code[]`；不在字符集内则报告字节偏移且不生成半成品。先把各码从高位写入位缓冲；完成后写 `CodeFile = CFM1 + B(64位有效位数) + ceil(B/8) 个载荷字节`，最后一个字节的未用低位必须填零。\n\n~~~text\nDecode(root, B, payload):\n  validate B 与载荷字节数及填充位\n  if root is leaf:\n    for each of B bits: require bit=0; emit root.ch\n    return\n  p = root\n  for i = 0 .. B-1:\n    p = (bit(i)=0 ? p.left : p.right); require p != nil\n    if p is leaf: emit p.ch; p = root\n  require p = root                 // 否则码字被截断\n~~~\n\n**D（译码）**：读取并校验 `CodeFile` 后调用 `Decode`，成功时才替换 `TextFile`。**P** 读同一位流，逐位输出并写 `CodePrin`，每行恰 50 位（最后一行可不足 50）。**T** 对树作右子树、结点、左子树的缩进输出到终端和 `TreePrint`，叶同时显示字符、权值和码；单叶也显示码 `0`。\n\n**可复验样例**：执行 `I`：`A:5, B:2, C:1`。稳定合并得到 `A=1,B=01,C=00`。`ToBeTran=ABCA` 时 `E` 产生有效位数 `B=6` 和位串 `101001`；执行 `D` 后 `TextFile` 必须逐字节为 `ABCA`。若把末位改为 `1`，内部树会在码字中途结束，`D` 必须报损坏。题给频度表也应把空格作为一个字符；其具体位串依赖已声明的稳定平局规则，但回译必须与 `THIS PROGRAM IS MY FAVORITE` 完全相同。\n\n**复杂度**：建树 `O(n log n)`，序列化 `O(n)`，建码表 `O(nh)`（`h` 为树高），编码为 `O(输入字节数+总码长)`，译码 `O(B)`；树、码表和位流分别占 `O(n)`、`O(nh)`、`O(B)` 空间。",
          "explanation": "提供覆盖 I/E/D/P/T/Q 的完整文件协议与算法，吸收审校中单叶树、稳定平局、有效位数、原子载入和严格译码校验。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。；已吸收上一版修正：原补充答案一面把 n 小于 2 判为非法，一面又规定 n 等于 1 的码字，且其通用译码路径不能处理单叶根。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入完整哈夫曼编译码器的独立设计与核验方法。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-3",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n按题设用邻接多重表保存连通无向图。每条无向边只分配一个 EBox(ivex,jvex,ilink,jlink)，并同时链接到两个端点的 firstedge 链；从顶点 v 沿边 e 的后继为 e 的 ivex 等于 v 时 ilink，否则 jlink。输入顶点编号须为 1 到 n，n 不超过 30；拒绝自环、重复边和不存在端点。将每个邻接链按另一端编号升序插入，输出序列就不依赖输入边顺序。\n\n~~~~c\nvoid DFS(const AMLGraph *G,int v) {\n  visited[v]=true; PrintVertex(v);\n  for (EBox *e=G->v[v].firstedge; e; e=NextAt(e,v)) {\n    int w=OtherEnd(e,v);\n    if (!visited[w]) { PrintTreeEdge(v,w); DFS(G,w); }\n  }\n}\nvoid BFS(const AMLGraph *G,int s) {\n  InitQueue(&Q); visited[s]=true; EnQueue(&Q,s);\n  while (!QueueEmpty(Q)) {\n    int v; DeQueue(&Q,&v); PrintVertex(v);\n    for (EBox *e=G->v[v].firstedge; e; e=NextAt(e,v)) {\n      int w=OtherEnd(e,v);\n      if (!visited[w]) { visited[w]=true; PrintTreeEdge(v,w); EnQueue(&Q,w); }\n    }\n  }\n}\n~~~~\n\n每次开始一种遍历都清空 visited，检查用户起点合法后执行。树边必须在首次发现 w 时按 (v,w) 输出，方向是父到子，不能按无向边的存储端点颠倒。DFS 的递归调用和 BFS 的入队标记均保证每个顶点只发现一次；连通性保证访问 n 个顶点并输出 n-1 条树边。每条边从两个端点各检查一次，时间 O(n+m)，visited、递归栈或队列额外空间 O(n)，图空间 O(n+m)。",
        "explanation": "严格采用邻接多重表，明确树边方向、确定性邻接顺序、入队标记和复杂度。",
        "correction": {
          "verified": "## 解法\n\n用邻接多重表保存无向图：每条边只建一个 `EBox{ivex,jvex,ilink,jlink}`，并接到两个端点的 `firstedge` 链。`NextAt(e,v)` 在 `v==ivex` 时返回 `ilink`，否则在 `v==jvex` 时返回 `jlink`；`OtherEnd(e,v)` 返回另一个端点。输入 `n,m` 和 `m` 条边，要求 `1<=n<=30`、端点存在、无自环和重复边。把每个顶点的关联边按另一端编号升序插入，使结果与输入边顺序无关；若一次 DFS 访问数小于 `n`，报告图不连通。\n\n~~~c\nvoid DFS(AMLGraph *G, int v) {\n  visited[v]=true; printVertex(v);\n  for (EBox *e=G->adjmulist[v].firstedge; e; e=NextAt(e,v)) {\n    int w=OtherEnd(e,v);\n    if (!visited[w]) { printTreeEdge(v,w); DFS(G,w); }\n  }\n}\nvoid BFS(AMLGraph *G, int s) {\n  InitQueue(&q); visited[s]=true; EnQueue(&q,s);\n  while (!QueueEmpty(q)) {\n    int v; DeQueue(&q,&v); printVertex(v);\n    for (EBox *e=G->adjmulist[v].firstedge; e; e=NextAt(e,v)) {\n      int w=OtherEnd(e,v);\n      if (!visited[w]) { visited[w]=true; printTreeEdge(v,w); EnQueue(&q,w); }\n    }\n  }\n}\n~~~\n\n菜单读取起点后分别 `memset(visited,0)` 再调用 DFS、BFS。树边在**首次发现**子结点时按 `(父,子)` 输出，不能按无向边存储端点输出。队列容量至少为 `n`，入队前检查满；每种遍历结束检查访问数为 `n`，否则明确显示未访问顶点。\n\n**可复验样例**：`n=5`，边按 `{(1,2),(1,3),(2,4),(3,4),(4,5)}`，起点 `1`。升序邻接下 DFS 序列为 `1 2 4 3 5`，生成树边为 `(1,2),(2,4),(4,3),(4,5)`；BFS 序列为 `1 2 3 4 5`，树边为 `(1,2),(1,3),(2,4),(4,5)`。\n\n**正确性与复杂度**：DFS 递归和 BFS 的发现即标记都使每个顶点至多成为一个树边的子端点；连通图因而产生 `n-1` 条有向树边并访问全部顶点。每条无向边至多从两端检查一次，时间 `O(n+m)`，附加空间 `O(n)`，图空间 `O(n+m)`。",
          "explanation": "以邻接多重表给出 DFS/BFS 的可执行核心，明确生成树边方向、输入校验和连通性处理。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入无向图 DFS、BFS 和生成树边集的独立实现。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-4",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n将课程号映射为 0 到 n-1 的内部下标，记录学分、后继表和直接先修表。读入时检查课程号为三个字母数字字符、学分为正、先修课程存在且不重复；某课程学分大于单学期上限 C，或总学分大于 T 乘 C 时立即无解。对先修图做 Kahn 拓扑检查；若不能输出全部 n 门课，存在环而无解。\n\n为避免贪心策略把暂时装不下误报为全局无解，按学期作精确回溯。状态为已完成集合 done、当前学期 s 和前 s-1 学期负担。可选集 A 是所有先修课均在 done 中的未排课程；枚举 A 的所有非空子集 S，只保留总学分不超过 C 的候选，下一状态为 done 并 S。若 s 大于 T 仍有未排课程则失败；若剩余学分除以 C 向上取整大于剩余学期数则剪枝。所有课程排完即输出每学期课程号、课程名、学分和合计。尚有课程时空学期不会帮助先修关系或容量约束，故不枚举。\n\n均衡策略在所有成功计划中最小化各学期负担对平均负担的平方偏差和，以较早安排作平局；集中策略按 (load1,load2,...,loadT) 字典序最大化。候选组合被完整枚举，故未找到才报告无解。用分支上界和状态记忆化加速；一般带容量先序调度是 NP 困难，最坏时间指数级，图、状态和输出空间 O(n+T)。不变量是已排课各先修课均在更早学期、每期总学分不超 C；任一合法计划都对应一个 S 分支，因而算法不漏解也不输出违规计划。",
        "explanation": "给出课程图校验、两种策略的明确定义，以及不会将贪心失败误报为无解的完整搜索。",
        "correction": {
          "verified": "## 解法\n\n输入为学期数 `T`、每期上限 `C` 与课程记录 `(三位课程号, 学分, 直接先修号表)`。先将课程号映射到 `0..n-1`，保存 `credit[]`、`pre[]`、`next[]`。拒绝重复课程号、非法课程号、非正学分、未知或重复先修号；若任一课程学分大于 `C`、总学分大于 `T*C`，立即无解。随后用 Kahn 拓扑排序检查先修图；未输出全部课程即存在环而无解。\n\n不能用一次贪心：可能某个局部装载选择会堵死后续课程。下面的回溯穷尽每个合法学期的课程集合，输出才可作为“无解”的依据。\n\n~~~text\nSearch(s, done, loads, plan):\n  if done has all n courses: Evaluate(plan, loads padded with zero to T); return\n  if s > T: return\n  if ceil(sum credit of unfinished / C) > T-s+1: return\n  A = { i not in done | every p in pre[i] is in done }\n  for every nonempty subset S of A in lexicographic course-id order:\n    w = sum(credit[i] for i in S)\n    if w <= C:\n      plan[s]=S; loads[s]=w\n      Search(s+1, done union S, loads, plan)\n      undo S\nEvaluate(plan, loads):\n  if strategy=balanced: minimize sum_i (loads[i]-W/T)^2\n  else: maximize tuple(loads[1],loads[2],...,loads[T]) lexicographically\n  break ties by whole plan's course-id sequence; retain best\n~~~\n\n尚有课程时不枚举空学期：它不改变 `done`，删除该空期并把之后课程前移不会破坏“先修课在更早学期”或上限，且不会恶化两个目标。结果写入用户指定文件，每期一行：`学期 | 课程号(学分) | 合计`。无记忆化实现的活动状态为回退用的集合、负担和计划；若要缓存，键必须是 `(s,done,loads[1..s-1])`，**不能**只用 `(s,done)`，因为两个策略都依赖既往负担。\n\n**可复验样例**：`T=3,C=5`，课程为 `C01:3:-`，`C02:2:-`，`C03:4:C01`，`C04:1:C02`。集中策略输出 `S1 C01,C02 (5); S2 C03,C04 (5); S3 空 (0)`；均衡策略的同一解负担为 `5,5,0`。若把 `C03` 改为 `6`，立即报告单课超过上限；若令 `C01` 先修 `C03`，拓扑检查报告环。\n\n**正确性与复杂度**：不变量是每门已排课程只出现一次、其所有先修课在严格更早期、每期负担不超 `C`。任一合法计划逐期的课程集合都会被枚举，故搜索不会漏解；所有成功分支都满足不变量。带容量先序调度一般为 NP 困难，最坏时间指数级；图空间 `O(n+m)`，无记忆化活动空间 `O(n+T)`，记忆化表空间随实际状态数 `M` 为 `O(M(n+T))`。",
          "explanation": "保留审校对精确搜索、两个策略的明确定义及安全记忆化键的修正，并补齐输入、输出和可复验实例。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。；已吸收上一版修正：原补充答案同时建议记忆化、却把状态图空间写成 O(n+T)，且只说明 done 与学期不足以安全合并依赖负担历史的优化状态。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入带先修关系和学分上限的精确教学计划编制方案。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-5",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n建立无向带权校园图。顶点记录 code、name、introduction，边记录 to、length、roadType、scenery；代码唯一，长度必须为正。可先录入至少十个景点，例如主校门、图书馆、教学楼、实验楼、食堂、宿舍、体育馆、校医院、行政楼、湖畔；道路在两个端点的邻接表中各存一条。按代码建立哈希索引，景点信息查询为索引定位后输出名称、简介和相邻道路。\n\n问路时检查起点、终点存在；相同则返回零长度、零边路径。对正权无向网运行带最小堆的 Dijkstra：dist[s]=0，每次取未确定且 dist 最小的 u；对每条 (u,v,w)，若 dist[u]+w 小于 dist[v] 则更新 dist 和 prev[v]=u。相等距离可按顶点 code 保留较小前驱。终点不可达时报无路；否则从终点沿 prev 回溯到起点后逆置，输出一条最短路径、总长度和逐段道路。边长为正且每次松弛使距离严格增大，prev 链没有环，所得路径是简单路径。\n\nDijkstra 的已确定顶点满足最短距离不再可能改善，非负边权保证该贪心性质。信息查询平均 O(1)；问路 O((V+E) log V)，附加空间 O(V)，图空间 O(V+E)。若增删景点或道路，须同步维护双向边、索引和正权约束；删除景点时删除所有关联边。",
        "explanation": "覆盖不少于十个景点、无向网、信息查询、不可达和一条最短简单路径。",
        "correction": {
          "verified": "## 解法\n\n建立无向带权校园图。景点为 `Spot{code,name,introduction}`，道路为两个邻接表项 `Road{to,length,type,scenery,direction}`；`code` 唯一，`length>0`。初始化至少录入十个景点：主校门、图书馆、教学楼、实验楼、食堂、宿舍、体育馆、校医院、行政楼、湖畔；用哈希表 `code -> 顶点号` 支持查询。新增道路必须两个端点存在且尚无同类道路；删除景点时先删其所有关联道路并更新索引。\n\n信息查询先查索引，不存在则报错；存在则输出名称、简介和相邻道路。问路也先验证两端：同一景点直接返回长度 0 的空路径；不可达则返回无路。\n\n~~~text\nShortest(s,t):\n  for v: dist[v]=INF, prev[v]=-1\n  dist[s]=0; push (0,s) into minHeap\n  while heap not empty:\n    d,u=popMin(); if d!=dist[u]: continue\n    for road in adj[u]:\n      v=road.to; nd=d+road.length\n      if nd<dist[v] or (nd=dist[v] and code[u]<code[prev[v]]):\n        dist[v]=nd; prev[v]=u; push(nd,v)\n  if dist[t]=INF: report unreachable\n  reconstruct t -> prev -> s, reverse, output spots and each road\n~~~\n\n所有道路长度为正，前驱距离严格递减，回溯路径不会有环，因此得到一条最短简单路径。相等距离使用前驱代码的固定规则，保证答案可重复。\n\n**可复验样例**：登记 `GATE,LIB,TEACH,LAB,CANTEEN,DORM,GYM,HOSP,ADMIN,LAKE` 十点；加入 `GATE-LIB:200, LIB-TEACH:100, TEACH-LAB:120, GATE-ADMIN:150, ADMIN-LAB:300`。查询 `GATE -> LAB` 输出 `GATE -> LIB -> TEACH -> LAB`，总长 `420`，而非经 `ADMIN` 的 `450`；查询不存在的 `X` 报景点不存在。\n\n**正确性与复杂度**：Dijkstra 每次确定当前未确定顶点中的最短距离，非负边保证该距离不再被改善。景点查询平均 `O(1)`；问路 `O((V+E)log V)`，前驱和堆的附加空间 `O(V)`，图空间 `O(V+E)`。",
          "explanation": "给出含十景点、查询、增删、最短简单路径、错误处理和稳定输出的完整校园导游核心。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入校园导游的图模型和最短路查询独立方案。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-6",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n按题意把通信网建成无向网的边数组 Edge(u,v,w)，顶点数不超过 30，权为小于 100 的整数。输入时检查端点存在、u 不等于 v、权值合法；平行边可保留，克鲁斯卡尔会自然选择合适者。MFSet 严格采用教材 6.5 节的双亲表表示，即 PTree：S.nodes[1..S.n] 的 parent 为负时保存集合大小，为正时保存双亲下标。\n\n~~~~c\nint Find(MFSet *S,int x) {\n  if (x<1 || x>S->n) return -1;\n  int r=x;\n  while (S->nodes[r].parent>0) r=S->nodes[r].parent;\n  while (x!=r) { int p=S->nodes[x].parent; S->nodes[x].parent=r; x=p; }\n  return r;\n}\nStatus Merge(MFSet *S,int i,int j) {  /* i、j 必须是不同集合的根 */\n  if (i<1 || i>S->n || j<1 || j>S->n || i==j) return ERROR;\n  if (S->nodes[i].parent>S->nodes[j].parent) { int t=i; i=j; j=t; }\n  S->nodes[i].parent+=S->nodes[j].parent;\n  S->nodes[j].parent=i; return OK;\n}\nStatus Kruskal(Edge e[],int m,int n) {\n  Initial(&S,n,1,2,...,n); SortByWeightThenEndpoints(e,m); int count=0;\n  for (int k=0;k<m && count<n-1;++k) {\n    int a=Find(&S,e[k].u), b=Find(&S,e[k].v);\n    if (a!=b) { Print(e[k].u,e[k].v,e[k].w); Merge(&S,a,b); ++count; }\n  }\n  return count==n-1 ? OK : ERROR;\n}\n~~~~\n\nn 为 0 视为非法，n 为 1 的最小生成树为空且成功；循环结束不足 n-1 条边说明图不连通，应报告无生成树而非把森林当答案。每次选中边连接不同分量，是当前跨某割的最轻可选边，满足割性质；最终选 n-1 条无环边即为最小生成树。排序 O(m log m)，按大小合并加路径压缩的并查集操作总计 O(m alpha(n))，空间 O(m+n)；若选作使用堆排序，排序项仍为 O(m log m)。",
        "explanation": "严格使用边数组和 MFSet，处理连通性、平行边、单顶点和最小生成树输出。",
        "correction": {
          "verified": "## 解法\n\n把通信网存为边数组 `Edge{u,v,w}`，其中 `1<=u,v<=n<=30`、`u!=v`、`0<=w<100`；允许平行边，拒绝非法端点和自环。MFSet 使用教材式双亲表：根的 `parent` 为负集合大小，非根的 `parent` 为父下标。\n\n~~~c\nint Find(MFSet *s,int x) {\n  if (x<1 || x>s->n) return -1;\n  int r=x; while (s->node[r].parent>0) r=s->node[r].parent;\n  while (x!=r) { int p=s->node[x].parent; s->node[x].parent=r; x=p; }\n  return r;\n}\nint Merge(MFSet *s,int a,int b) {\n  if (a<1 || b<1 || a==b) return ERROR;\n  if (s->node[a].parent>s->node[b].parent) { int t=a; a=b; b=t; }\n  s->node[a].parent += s->node[b].parent; s->node[b].parent=a; return OK;\n}\nint Kruskal(Edge e[],int m,int n) {\n  if (n<1) return ERROR; InitMFSet(&S,n);\n  sort e by (w,u,v); int cnt=0, total=0;\n  for (int k=0;k<m && cnt<n-1;k++) {\n    int a=Find(&S,e[k].u), b=Find(&S,e[k].v);\n    if (a!=b) { print(e[k]); total+=e[k].w; Merge(&S,a,b); cnt++; }\n  }\n  if (cnt!=n-1) return ERROR;\n  printTotal(total); return OK;\n}\n~~~\n\n`n=1` 成功并输出空树、总权 0；循环结束尚不足 `n-1` 条边时只能输出“图不连通，无最小生成树”，不能把森林当作答案。\n\n**可复验样例**：`n=4`，边为 `(1,2,1),(2,3,2),(1,3,4),(3,4,3),(2,4,5)`。输出依次为 `(1,2,1),(2,3,2),(3,4,3)`，总权 `6`。删除 `(3,4,3)` 和 `(2,4,5)` 后顶点 4 不连通，程序报告无生成树。\n\n**正确性与复杂度**：每次加入连接两个不同分量的当前最轻边，满足割性质；并查集禁止成环。选到 `n-1` 条边即为连通无环的最小生成树。排序 `O(m log m)`，按大小合并与路径压缩的并查集操作总计 `O(m alpha(n))`，空间 `O(m+n)`。",
          "explanation": "提供 MFSet、克鲁斯卡尔完整核心和连通性、单顶点、平行边等边界处理。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入克鲁斯卡尔算法与并查集的独立解答。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-7",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\nExpression 采用二叉树：叶为单个数字 0 到 9 或变量 a 到 z，内部结点为二元运算符 +、-、*、/、^。ReadExpr 从前缀串当前位置递归读取：叶读一个字符；遇运算符则先建结点，再依次读取左、右子树；忽略空白并要求最终恰读完整串。因此 -91 解析为 (9-1)，+a*bc 解析为 (a+(b*c))。变量表 value[26] 初值全为 0，Assign 仅接受合法变量并更新对应单元。\n\n~~~~c\nNode *ReadExpr(const char **p) {\n  SkipSpace(p); if (**p=='\\0') return NULL;\n  char c=*(*p)++;\n  if (c>='0' && c<='9') return NewConst(c-'0');\n  if (c>='a' && c<='z') return NewVar(c);\n  if (!IsOp(c)) return NULL;\n  Node *l=ReadExpr(p),*r=ReadExpr(p);\n  return l && r ? NewOp(c,l,r) : NULL;\n}\ndouble Value(Node *x) {\n  if (x->tag==CONST) return x->number;\n  if (x->tag==VAR) return value[x->name-'a'];\n  double a=Value(x->left),b=Value(x->right);\n  if (x->op=='/' && b==0) Error();\n  if (x->op=='^' && a==0 && b<=0) Error();\n  return Apply(x->op,a,b);\n}\n~~~~\n\nWriteExpr 对叶直接输出，对内部结点输出左括号、左子树、运算符、右子树、右括号；全括号形式语义唯一。Value 是后根求值，除零和零的非正幂报错。CompoundExpr 先验证运算符，再用 Clone(E1)、Clone(E2) 建新根，避免新树与原树共享可释放结点；若改为移交所有权，必须在接口中明示。读入、输出和求值均 O(N) 时间，树空间 O(N)，递归栈 O(height)。结构归纳说明每个子树求得其对应前缀子式的值，故根值正确。",
        "explanation": "覆盖前缀解析、全括号中缀输出、变量赋值、后根求值、异常与复合表达式所有权。",
        "correction": {
          "verified": "## 解法\n\n表达式是二叉树：叶为一位常量 `0..9` 或变量 `a..z`，内部结点为 `+ - * / ^`。输入前缀式时忽略空格；读完根后必须恰好到输入末尾，任何缺操作数、非法字符或多余字符都报错并释放已建子树。变量表 `value[26]` 初值全为 0，`Assign(v,c)` 仅接受 `a..z` 和有限实数。\n\n~~~c\nNode *Read(const char **p, bool *ok) {\n  SkipSpace(p); if (**p=='\\0') { *ok=false; return NULL; }\n  char c=*(*p)++;\n  if (c>='0' && c<='9') return NewConst(c-'0');\n  if (c>='a' && c<='z') return NewVar(c);\n  if (!IsOp(c)) { *ok=false; return NULL; }\n  Node *l=Read(p,ok), *r=Read(p,ok);\n  if (!*ok || !l || !r) { Free(l); Free(r); return NULL; }\n  return NewOp(c,l,r);\n}\ndouble Value(Node *x, bool *ok) {\n  if (x->tag==CONST) return x->number;\n  if (x->tag==VAR) return value[x->name-'a'];\n  double a=Value(x->left,ok), b=Value(x->right,ok);\n  if (!*ok || (x->op=='/' && b==0) || (x->op=='^' && a==0 && b<=0)) { *ok=false; return 0; }\n  return Apply(x->op,a,b);\n}\n~~~\n\n`WriteExpr` 对叶直接输出，对内部输出 `(`、左子树、运算符、右子树、`)`，得到无歧义的全括号中缀式。`CompoundExpr(P,E1,E2)` 先验证 `P`，再以 `Clone(E1)`、`Clone(E2)` 建新根，故释放新树或原树均不会悬挂；若实现采用所有权转移，接口必须明确原树失效。\n\n**可复验样例**：读 `+a*bc` 后 `WriteExpr` 输出 `(a+(b*c))`。赋值 `a=2,b=3,c=4`，`Value` 输出 `14`。读 `-91` 输出 `(9-1)`、值为 `8`；读 `/90` 时 `Value` 报除零，不返回伪结果。\n\n**正确性与复杂度**：前缀运算符递归恰读取两棵子树，故构造树与输入式一一对应；后根求值先取得两个子式值再执行根运算，归纳可得结果正确。读入、输出、求值、克隆均为 `O(N)` 时间，树空间 `O(N)`，递归栈 `O(height)`。",
          "explanation": "覆盖表达式五项基本操作，补齐前缀格式校验、异常、深拷贝所有权和样例。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入二叉表达式类型全部基本操作的独立解答。"
      }
    },
    {
      "id": "book-ds-yan-practice-5-5-8",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n题面要求计算中转等待时间，却没有显式给出查询出发时刻；程序界面必须增加 t0（出发日期时间）和查询截止时刻 H，否则最快没有唯一含义。城市表以唯一代码索引；每条班次/车次记录为 id、from、to、mode、depart、arrive、fare，时间使用绝对日期时间并要求 arrive 大于 depart、fare 非负。火车的每一区段建成一条有发到时刻和票价的服务边；编辑可从文件或键盘新增、删除城市和服务，删除城市时先删关联服务。\n\n对一次查询先按用户选定交通工具过滤服务，并构造 t0 到 H 的时间扩展邻接表：每个 (城市,事件时刻) 为顶点；同城相邻事件以等待边相连，时间代价为时间差、金钱代价为 0；每个班次以乘坐边连接发车、到达事件，时间代价为 arrive-depart、金钱代价为 fare；源点连到起点 t0 后的首个事件。若时刻表每日循环，先在 [t0,H] 展开相应日期的班次，不能无限展开。\n\n最快查询在该图上以时间代价运行 Dijkstra，终点城市最小标签减 t0 即总耗时，等待边自动计入中转等待。最省钱查询在同一图上以金钱代价运行 Dijkstra，等待边为零；这避免把便宜但到得太晚、赶不上后续班次的前缀误判为可行最优路径。保存 predecessor，回溯后按时间输出每段的出发时刻、车航班号、到达城市和时刻；无终点事件或超过 H 则报告无可行行程。只保留选定 mode，满足全程单一交通工具。\n\n两种权值非负，Dijkstra 正确；时间扩展图把等待显式化，使可行路径与实际行程一一对应。若扩展图有 Vt 个事件、Et 条边，查询时间 O((Vt+Et) log Vt)，空间 O(Vt+Et)。",
        "explanation": "补足最快咨询必要的出发时刻和时域，并正确处理等待、最早到达、最省钱和单一交通工具限制。",
        "correction": {
          "verified": "## 解法\n\n城市表按唯一城市代码索引；服务表为 `Service{id,from,to,mode,depart,arrive,fare}`，`mode` 只能为 `train/plane`，`arrive>depart`、`fare>=0`。火车的每一行驶区段登记为一条服务边并保留同一车次号；飞机按航班登记。城市和服务的新增、删除均提供键盘录入与文件导入两种入口；删城市时同时删其关联服务，所有修改后重新校验端点和时间。\n\n一次咨询输入 `start,end,目标(time/money),mode,t0,H`；`t0` 是绝对出发时刻，`H>=t0` 是查询截止时刻。没有 `t0` 就无法定义初始等待或最快到达。先按 `mode` 过滤服务；若时刻表每日循环，仅展开 `[t0,H]` 内的具体日期实例，跨日到达仍用绝对时间。\n\n构造时间扩展图。每个 `(city,time)` 为事件顶点，起点额外放入 `(start,t0)`；同城事件按时间排序，相邻事件连等待边，时间权为时间差、金钱权为 0；每条服务连乘坐边 `(from,depart)->(to,arrive)`，时间权为 `arrive-depart`、金钱权为 `fare`。因此从 `(start,t0)` 到首班服务的等待被明确计入，不能把超级源以零代价直接连到首班。\n\n~~~text\nConsult(start,end,objective,mode,t0,H):\n  validate cities, objective, mode, t0<=H; if start=end: return empty trip, 0, 0\n  G = ExpandAndBuildEvents(filtered services, t0, H)\n  weight(e) = (e.time, e.money) if objective=time else (e.money, e.time)\n  run Dijkstra from event(start,t0) with lexicographic nonnegative weights\n  choose the minimum-label reachable event(city=end,time<=H)\n  if none: report no feasible itinerary\n  follow predecessor edges backward and reverse\n  print every ride: departure city/time, service id, arrival city/time, fare\n  print total elapsed time and total fare; optionally print waiting edges\n~~~\n\n最省钱目标把 `(钱数,总时间)` 作为权重；次关键字只用于稳定输出，不改变“钱最少”。每次松弛存前驱顶点和服务指针，回溯即可核验每次换乘的等待时间。\n\n**可复验样例**：查询 `A->C`、`mode=train`、`t0=08:00`、`H=14:00`。服务 `T1:A 09:00 -> B 10:00,50`，`T2:B 10:30 -> C 12:00,40`，另有 `T3:A 08:15 -> C 13:30,20`。最快结果为 `T1,T2`，总耗时 `4小时`（含初等 1 小时和中转 30 分钟），总费 90；最省钱结果为 `T3`，费用 20、总耗时 `5小时30分`。若只选 `plane` 而无航班，报告该时域内无可行行程。\n\n**正确性与复杂度**：实际行程与事件图中交替的等待/乘坐边一一对应，边按时间不减且服务时刻相容；反向也成立。所有权值非负，Dijkstra 给出相应目标最优解。若展开后有 `Vt` 个事件、`Et` 条边，单次查询为 `O((Vt+Et)log Vt)` 时间和 `O(Vt+Et)` 空间。",
          "explanation": "给出可运行的时刻表查询模型，吸收审校对 t0/H、单一交通工具、时间扩展图和初始等待不可漏算的修正。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。；已吸收上一版修正：原补充答案把源点连到 t0 之后的首个事件却未定义该边的时间代价，默认零权时会漏掉初始等候时间。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入带时刻表、等待时间和单一交通工具限制的可执行咨询方案。"
      }
    },
    {
      "id": "book-ds-yan-practice-6-6-1",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n以 m=5 管理 32 个 cell。为避免命令 -0 与结束命令 0 无法区分，交互界面对外公布地址 1 到 32，内部数组偏移为 publicAddress-1；归还命令 -a 因而可归还任意块。avail[k] 是长度为 2^k 的空闲块表，块头保存 kvalue 和内部 blkstart；allocated 用 rlink 链接已分配块，并保存实际分配阶而非仅保存用户申请量。初态仅有 avail[5] 的 start 0 块，allocated 为空，space 数组不存业务数据。\n\n申请 n 时，n 大于 32 失败且状态不变。求最小 k=ceil(log2 n)，从 k 到 m 找首个非空 avail 表；若无则失败。取出阶 l 块，反复分裂成左半和右半，右半插入 avail[l-1]，左半继续分裂，直到 l=k；左半块头进入 allocated 并显示全表。归还 -a 时先换算内部地址并在 allocated 精确查找起始地址；找不到、地址在块内但非块首均报错。摘下该块后令 buddy=base XOR 2^k；只要同阶空闲表中存在这个伙伴，就摘下伙伴、令 base 为较小起址、k 加一，直到无伙伴，再把合并块插入 avail[k]。各表按起址排序显示占用块的始址和长度，以及每个空闲阶的始址和长度。\n\n分裂保持每个 cell 恰属于一个块；合并只发生在同阶、相邻且同一父块的伙伴之间，故不重叠也不丢失空间。若空闲表按题给链表线性查找伙伴，单次操作为 O(m+F)，F 为扫描块头数，最坏 O(2^m)；若另建按 (k,start) 索引则为 O(m)。块头、空闲表、占用表总空间 O(2^m)，m=5 时为常数。最后的申请 40 必须失败且状态不变。",
        "explanation": "严格遵循伙伴分裂、伙伴合并和题给表结构，并处理 -0 与结束命令的接口歧义。",
        "correction": {
          "verified": "## 6.1 伙伴存储管理系统\n\n**约定。** 管理 `SpaceSize=2^5=32` 个 `cell`，内部地址为 `0..31`；为避免输入 `-0` 与结束命令 `0` 冲突，交互界面把块首址显示和接受为 `1..32`，内部地址为 `外部地址-1`。`avail[k]` 是大小 `2^k` 的空闲块链表，`allocated` 是已分配块链表；块头不占 `space` 中的 cell，只保存元数据。输入正数表示申请，负数表示归还其绝对值所指的**块首址**，0 结束。\n\n```text\nBlock = {start, k, next}                 // 长度为 2^k\navail[0..5]：按 start 升序的空闲链表\nallocated：按 start 升序的已分配链表\nInit(): avail[5] = [(0,5)]；其余为空；allocated 为空\n\nAllocate(n):\n  若 n<1 或 n>32：失败，状态不变\n  k=0；当 2^k<n 时 k++\n  在 l=k..5 中找第一个非空 avail[l]；若不存在：失败，状态不变\n  取出该块 b\n  当 l>k：l--；右半块 (b.start+2^l,l) 插入 avail[l]；b.k=l\n  将 b 插入 allocated；返回 b.start+1\n\nFree(publicStart):\n  s=publicStart-1；在 allocated 中精确查找 start==s 的块 b\n  找不到即报错（因此不会把块内部地址误当首址归还）\n  从 allocated 删除 b\n  while b.k<5:\n      mateStart = b.start XOR 2^b.k\n      若 avail[b.k] 中没有 start==mateStart 的块：break\n      删除该伙伴；b.start=min(b.start,mateStart)；b.k++\n  将 b 插入 avail[b.k]\n\nShow(): 输出 allocated 的 (外部首址,2^k)，再逐阶输出 avail[k] 的 (外部首址,2^k)\n```\n\n`XOR 2^k` 得到同一父块内、同阶的唯一伙伴；只有该伙伴当前也空闲时才合并，故每一步既不重叠也不丢失 cell。分裂后左半块继续分裂、右半块入空闲表，故得到的块大小恰为不小于请求的最小 2 的幂。\n\n**可复验样例。** 初态为 `F[5]=(1,32)`。依次输入 `1,3,4`：分配结果首址依次为 `1,5,9`，已占用为 `(1,1),(5,4),(9,4)`；空闲表至少含 `(2,1),(3,2),(13,4),(17,16)`。再输入 `-5` 后 `(5,4)` 被回收但不能与 `(1,1)` 合并；最后输入 `40` 必须报告申请超过 32，所有表完全不变。\n\n单次分裂或合并至多 5 层；若链表线性找伙伴，时间为 `O(5+F)`，`F` 为被扫描的同阶链表长度，最坏 `O(32)`；用 `(k,start)` 索引可为 `O(5)`。元数据空间 `O(32)`。",
          "explanation": "保留伙伴分裂、XOR 伙伴合并、占用表与空闲表显示的既有正确约束，并明确了 -0 歧义、精确归还、失败状态不变和可手算轨迹。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入伙伴存储分配、回收、状态显示和地址边界的独立设计。"
      }
    },
    {
      "id": "book-ds-yan-practice-6-6-2",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n把姓名规范为大写 ASCII 拼音并删除分隔空格、连字符；空串、超过 19 字符或重复姓名拒绝。选容量为素数 M，例如先取 101，使 30 个记录装填因子约为 0.297。先将字符码按折叠多项式累积，再作除留余数 home=fold(name) mod M。为满足伪随机探测且保证不漏槽，在建表时用固定种子 Fisher-Yates 生成 perm[0..M-1] 的随机排列，perm[0]=0；第 i 次探测为 (home+perm[i]) mod M。同一表保存同一 perm，查询才能重现插入路径。\n\n~~~~c\nint FindSlot(Table *T,const char *name,bool *found,int *probes) {\n  int h=Hash(name,T->M);\n  for (int i=0;i<T->M;++i) {\n    int p=(h+T->perm[i])%T->M; ++*probes;\n    if (T->a[p].state==EMPTY) { *found=false; return p; }\n    if (T->a[p].state==USED && Equal(T->a[p].name,name)) { *found=true; return p; }\n  }\n  return -1;\n}\n~~~~\n\nInsert 用 FindSlot 找到空位后写入，成功查找的 probes 即该记录的查找长度。建完 30 人后计算 ASL 为所有成功查找 probes 之和除以 30，只有 ASL 不大于 2 才接受该表；否则换下一个更大素数容量或新的固定种子重建，并记录最终 M、种子和 ASL。这样不会仅凭期望值声称满足上限。排列探测覆盖全部槽，所以表未满时查找或插入必终止；同一规范名沿同一路径命中。均匀散列假设下平均查找 O(1)，最坏 O(M)，空间 O(M)。",
        "explanation": "使用除留余数与可复现的伪随机全排列探测，并以实际成功查找长度验证 ASL 上限。",
        "correction": {
          "verified": "## 6.2 人名哈希表\n\n**输入与表项。** 输入至多 30 个拼音姓名；先删除空格和连字符并转为大写 ASCII，只接受 `A..Z`，长度须为 `1..19`，规范化后重复的姓名拒绝。表项为 `state∈{EMPTY,USED}`、`name[20]`。选素数容量 `M=101`，初始装填因子小于 0.30；若实测成功查找 ASL 大于 2，则换下一个更大的素数并重建。\n\n```text\nFold(s):\n  h=0\n  对 s 的每个字符 c：h=(h*131 + code(c)) mod 2147483647\n  return h\nHash(s)=Fold(s) mod M\n\nBuildPermutation(M, seed):\n  p=[0,1,...,M-1]\n  用确定性 Fisher-Yates 和 seed 打乱 p[1..M-1]，并令 p[0]=0\n  return p\n\nFindSlot(name):\n  h=Hash(name)\n  for i=0..M-1:\n      pos=(h+p[i]) mod M\n      probes++\n      若 table[pos] 为空：return (pos,false,probes)\n      若 table[pos].name==name：return (pos,true,probes)\n  return (-1,false,probes)\n\nInsert(name):\n  先规范化；非法则报错\n  (pos,found,q)=FindSlot(name)\n  若 found：报告重复；若 pos<0：报告表满\n  否则写入 USED/name，并保存该记录成功查找长度 q\nLookup(name)：规范化后调用 FindSlot；found 才成功，否则未找到\n```\n\n同一张表必须保存 `M`、`seed` 和排列 `p`，查询才能走与建表完全相同的伪随机探测序列。`p` 是 `0..M-1` 的一个排列，所以未满表一定会访问到空槽并终止；不能每次查询重新生成随机数。建表结束计算 `ASL=sum(每条记录的成功 probes)/30`，仅当 `ASL≤2` 接受该配置。\n\n**可复验样例。** 令 `M=11`、`p=[0,4,1,7,2,9,3,6,5,8,10]`，并假设 `Hash(LI)=3`、`Hash(WANG)=3`。插入 `LI` 占 3，插入 `WANG` 依次试 3、7，故占 7、其成功查找长度为 2；查询 `WANG` 也在第二次命中。查询 `ZHANG` 的路径遇到空槽即返回未找到。\n\n均匀散列假设下插入和查找平均 `O(1)`，最坏 `O(M)`，空间 `O(M)`；规范化和哈希另耗 `O(L)`，其中 `L≤19`。",
          "explanation": "吸收原方案的除留余数、可复现伪随机全排列探测和以实际成功探测次数验收 ASL 的做法，补足了规范化、终止性、重建和样例。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入姓名规范化、建表、查表、冲突处理和 ASL 验证。"
      }
    },
    {
      "id": "book-ds-yan-practice-6-6-3",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n以 2-3 树索引书号。每个结点含 1 或 2 个有序键及 0、2 或 3 个孩子；键指向 Book(no,title,author,total,available,loans)，借阅记录挂在对应 Book 后。Search 按键区间下降，直到命中或叶空位。入库命中时 total 和 available 同时增加；否则把新 Book 插入叶。叶已有两键时，把两旧键和新键排序，保留最小键在原结点、新建右兄弟存最大键，中键连同两个结点向父提升；父满则同样分裂直至新根。内结点分裂还须按顺序分配四个孩子给左右结点。\n\n清除库存先查找书号；若有未归还记录或 available 不等于 total 则拒绝注销，避免悬空借阅。若键在内结点，用左子树最大键或右子树最小键的 Book 指针替换，再在叶删除该键。叶删后若仍有一键结束；若下溢，优先向有两键的相邻兄弟借键，经父分隔键旋转一个键和相应孩子；两兄弟都仅一键时，与一侧兄弟及父分隔键合并成两键结点，并在父中删分隔键和一个孩子，必要时向上修复。根变为零键时将唯一孩子升为新根，或成为空树。借阅先 Search，现存量为零失败，否则 available 减一并登记证号、到期日；归还必须找到该书该证的未归还记录，删除登记后 available 加一且不得超过 total。\n\n显示使用 Print(node,depth)：打印 depth 级缩进和一或两书号，再从左到右递归孩子。每次插入题给序列及删除 45、90、50、22、42 后检查：非根结点有 1 或 2 键、孩子数为键数加一、键区间正确、叶同深度、中序有序且无重复。高度 O(log B)，查找、入库、清除 O(log B)；若 loans 是链表，单书归还定位 O(该书借阅数)，证号散列表可平均 O(1)。总空间 O(B+L)。",
        "explanation": "覆盖 2-3 树插入分裂、删除下溢修复、库存一致性、借还与凹入表验证。",
        "correction": {
          "verified": "## 6.3 基于 2-3 树的图书管理\n\n**数据结构与接口。** `Book={no,title,author,total,available,loans}`，其中 `loans` 为 `(card,due)` 链表或哈希表。2-3 树结点含递增的 1 或 2 个书号/Book 指针，内部结点的孩子数恒为键数加 1；全部叶子同深。接口为 `stock(no,title,author,count)`、`remove(no)`、`borrow(no,card,due)`、`returnBook(no,card)`、`show()`。`count<=0`、重复借同一书证、归还不存在借阅、库存不足均报错且不修改树。\n\n```text\nSearch(x,no):\n  在 x 的 1 或 2 个键中比较；命中返回 Book\n  未命中且非叶，按 no 所在区间下降；空树返回 NOT_FOUND\n\nInsert(root, book):\n  递归插入叶；若结点已有 3 个候选键，排序为 a<b<c\n  左结点保留 a，右兄弟保留 c，向父结点上送 (b,right)\n  父结点同样溢出则继续分裂；根溢出时新建根 [b]，孩子为 left/right\n\nStock(no,...,count):\n  b=Search(root,no)\n  若存在：b.total+=count；b.available+=count\n  否则：Insert(root,Book(no,...,count,count,空))\n\nBorrow(no,card,due):\n  b=Search；若不存在或 b.available==0 则失败\n  若 card 已在 b.loans 则失败；加入 (card,due)，b.available--\nReturnBook(no,card):\n  b=Search；删除对应 loan；若不存在则失败\n  b.available++；断言 b.available<=b.total\n\nRemove(no):\n  b=Search；不存在失败；若 b.available!=b.total 或 loans 非空则拒绝\n  若键在内部结点，用前驱（左子树最大键）替换，再到叶删除\n  叶删后若仍有 1 键结束；若下溢：\n    优先向有 2 键的相邻兄弟借键，经父分隔键旋转；\n    否则与一侧兄弟及父分隔键合并，并在父中删除该分隔键，必要时向上修复\n  根变为 0 键时提升其唯一孩子；无孩子则为空树\n```\n\n分裂保持键区间和同层叶子；借键或合并后每个非根仍有 1 或 2 键，因此上述修复后仍是 2-3 树。`show(node,depth)` 先打印 `depth` 级缩进和结点键，再从左到右递归孩子，可得到凹入表。每次插入、删除后应校验：中序严格递增、孩子数=键数+1、叶深一致。\n\n**可复验样例。** 从空树依次入库 `35,16,18`：叶 `[16,18,35]` 分裂，根为 `[18]`，两个孩子为 `[16]`、`[35]`。继续按题给序列入库；对书号 45 借出一册后执行清除 45 必须被拒绝；归还后才允许清除。题给删除顺序 `45,90,50,22,42` 中每步均打印并做不变式检查。\n\n树高为 `O(log B)`，查找、入库、注销为 `O(log B)`；借还另加借阅记录定位成本（链表 `O(loans)`，按书证哈希表平均 `O(1)`）；总空间 `O(B+L)`。",
          "explanation": "保留 2-3 树分裂、删除下溢修复与库存一致性要求，补足业务级失败条件、接口、校验和题给序列的验证方式。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入基于 2-3 树的图书管理基本业务的独立设计。"
      }
    },
    {
      "id": "book-ds-yan-practice-6-6-4",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n结点保存 key、左右孩子和 height，约定空树高为 0、叶高为 1，平衡因子 bf=height(left)-height(right)。查找按二叉查找树规则下降；重复关键字按接口约定拒绝或维护计数，不能再插入同 key 结点。插入或删除递归返回时先更新高度，再按 bf 作 LL、LR、RR、RL 旋转。\n\n~~~~c\nNode *Balance(Node *x) {\n  UpdateHeight(x);\n  if (BF(x)==2) { if (BF(x->left)<0) x->left=RotateLeft(x->left); return RotateRight(x); }\n  if (BF(x)==-2) { if (BF(x->right)>0) x->right=RotateRight(x->right); return RotateLeft(x); }\n  return x;\n}\nNode *Delete(Node *x,Key k) {\n  if (!x) return NULL;\n  if (k<x->key) x->left=Delete(x->left,k);\n  else if (k>x->key) x->right=Delete(x->right,k);\n  else if (!x->left || !x->right) { Node *c=x->left?x->left:x->right; Free(x); return c; }\n  else { Node *p=MinNode(x->right); x->key=p->key; x->record=p->record; x->right=Delete(x->right,p->key); }\n  return Balance(x);\n}\n~~~~\n\n删除含两个孩子结点时用右子树最小值替换，再把实际删除传到至多一个孩子的结点，符合题意；回溯 Balance 正是子树变矮后的平衡处理。找不到 key 时不改变树。每次插删后以凹入表显示，并校验中序严格有序、height 正确、所有 bf 属于 -1、0、1。旋转保持中序序列，Balance 恢复高度差，故操作后仍为 AVL。查找、插入、删除 O(log n)，递归栈 O(log n)，结点空间 O(n)。",
        "explanation": "给出可直接实现的 AVL 删除和四种旋转，覆盖空树、找不到键、替换结点与平衡校验。",
        "correction": {
          "verified": "## 6.4 AVL 动态查找表\n\n令空树高度为 0、叶高度为 1，`bf=height(left)-height(right)`。结点为 `Node={key,value,left,right,height}`；接口拒绝重复关键字，删除不存在关键字只报告未找到，均不破坏原树。\n\n```text\nH(x)=x ? x.height : 0\nUpdate(x): x.height=1+max(H(x.left),H(x.right))\nRotateRight(y): x=y.left; t=x.right; x.right=y; y.left=t; Update(y); Update(x); return x\nRotateLeft(x): y=x.right; t=y.left; y.left=x; x.right=t; Update(x); Update(y); return y\nBalance(x):\n  Update(x); b=H(x.left)-H(x.right)\n  若 b==2：若 H(x.left.left)<H(x.left.right)，x.left=RotateLeft(x.left)；return RotateRight(x)\n  若 b==-2：若 H(x.right.right)<H(x.right.left)，x.right=RotateRight(x.right)；return RotateLeft(x)\n  return x\n\nFind(x,k): 循环按 k 与 x.key 比较向左/右，命中返回结点，否则 NOT_FOUND\nInsert(x,k,v):\n  若 x 为空：返回新叶；k 小/大则递归接到左/右；相等报重复\n  return Balance(x)\nDelete(x,k):\n  若 x 为空：返回 NOT_FOUND 标志\n  k 小/大则递归删左/右\n  命中时：\n    若仅 0 或 1 个孩子：释放 x，返回其唯一孩子\n    否则 s=右子树最小结点；复制 s 的 key/value 到 x；x.right=Delete(x.right,s.key)\n  若 x 非空：return Balance(x)\n```\n\n两孩子删除使用右子树最小键，把实际物理删除降至至多一个孩子的结点；递归回溯中的 `Balance` 正是子树变矮后的修复，覆盖 LL、LR、RR、RL 四种情形。旋转不改变中序序列，更新高度后所有结点的平衡因子恢复到 `-1,0,1`。\n\n**可复验样例。** 依次插入 `30,20,10` 后根应为 20（LL）；插入 `25,28` 后检查中序为 `10,20,25,28,30`。删除 20 时以右子树最小键 25 代换，删除后中序为 `10,25,28,30`，每个结点 `abs(bf)≤1`。删除 99 返回未找到，树的中序序列不变。\n\n查找、插入、删除均为 `O(log n)`，递归栈 `O(log n)`，结点空间 `O(n)`。凹入显示可先递归右子树、打印缩进和键、再递归左子树，插入/删除后调用它并验证中序、height、bf。",
          "explanation": "保留已有 AVL 删除中继替换和四类旋转的正确设计，补成可直接实现的旋转、维护、异常语义与验算样例。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入动态 AVL 查找表三项基本操作的独立解答。"
      }
    },
    {
      "id": "book-ds-yan-practice-6-6-5",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n采用不限定层数的字符 Trie。根和普通前缀结点含 27 条孩子指针：a 到 z 的 26 条及终止符美元符号；普通结点不存完整词，终止符孩子是叶结点，叶中恰存一个 Entry(word,phonetic,meaning)。因此 car 与 cart 分别在 c-a-r 的终止叶和 c-a-r-t 的终止叶存储，仍满足每个叶只含一个关键字。输入先统一为小写字母，空词和非字母拒绝。\n\n~~~~text\nInsert(word,e): 沿 word 的字符创建或进入普通前缀结点；若终止孩子已存在则报告重复或更新释义；否则挂一个只含 e 的叶。\nFind(word): 沿字符下降，最后检查终止孩子；存在则返回该叶。\nDelete(word): 保存路径；删除末尾终止叶后自底向上删除没有任何孩子的普通结点，根永不删除。\nPrefix(p): 沿 p 下降；从该结点 DFS 所有终止叶，按 a 到 z 次序输出完整 Entry。\n~~~~\n\nPrefix 结果包括恰好等于前缀的单词；路径不存在时返回空结果。删除 car 不会删除 cart 仍需的 c-a-r 前缀，因为其还有 t 孩子。每个词按字符唯一地到达终止叶，故完整查询、插入、删除正确；前缀 DFS 恰枚举以 p 开头的全部词。设词长 L，完整操作 O(L)，前缀查询 O(|p| 加输出字符数)，空间 O(全部不同前缀字符数加词条数)。",
        "explanation": "采用终止符叶来同时满足单词前缀关系和每个叶仅存一个关键字的题设。",
        "correction": {
          "verified": "## 6.5 英语词典 Trie\n\n采用无限层、逐字符分割的 Trie。普通前缀结点有 `child[27]`：`0..25` 对应 `a..z`，第 26 条边是终止符 `$`；终止符指向叶结点 `Entry={word,phonetic,meaning}`。因此普通结点不保存完整单词，每个叶子恰含一个关键字；`car` 位于 `c-a-r-$`，`cart` 位于 `c-a-r-t-$`。输入单词转小写，仅接受非空英文字母；释义和音标可为非空字符串。\n\n```text\nInsert(word,entry):\n  w=Normalize(word)，非法则失败\n  p=root\n  对 w 中每个字符 c：若 p.child[c] 为空则新建前缀结点；p=p.child[c]\n  若 p.child[$] 非空：报告重复（或按明确的 update 命令更新释义）\n  否则 p.child[$]=new Leaf(entry)\n\nFind(word):\n  w=Normalize；沿字符边下降，任一边为空即 NOT_FOUND\n  返回末结点的 child[$]，为空则 NOT_FOUND\n\nDelete(word):\n  下降时把 (parent,index) 压栈；若末尾没有 $ 则 NOT_FOUND\n  删除 $ 叶；自底向上：只要当前前缀结点没有任何孩子且不是 root，就从父结点删除它；遇到仍有孩子即停止\n\nPrefix(prefix):\n  p=root；沿 prefix 下降，失败返回空列表\n  DFS(p)：先访问 $ 叶，再按 a..z 次序递归子边，收集完整 Entry\n```\n\n查询完整词只在终止符叶存在时成功，所以 `car` 不是因 `cart` 存在而误命中。删除 `car` 仅删除 `$` 叶；若 `t` 边仍在，`c-a-r` 前缀结点不会被删。前缀检索先访问 `$` 可保证前缀本身也是结果。`DFS` 的 a 到 z 顺序同时给出字典序输出。\n\n**可复验样例。** 依次插入 `car /kɑː/ 汽车`、`cart /kɑːt/ 手推车`、`cat /kæt/ 猫`。`Find(car)` 返回汽车；`Prefix(ca)` 返回 `car,cart,cat`；删除 `car` 后 `Find(car)` 失败、`Find(cart)` 仍成功，`Prefix(ca)` 返回 `cart,cat`。插入 `ca-t` 必须因非法字符拒绝。\n\n词长为 `L` 时，插入、完整查找和删除时间为 `O(L)`；前缀查询为 `O(|prefix|+输出字符数)`；空间为所有不同前缀字符数加词条数。",
          "explanation": "保留终止符叶以满足每叶仅一个关键字的设计，补足输入规则、清理逻辑、前缀枚举次序及 car/cart 验证。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入无限层 Trie 的插入、删除、完整查询和前缀查询。"
      }
    },
    {
      "id": "book-ds-yan-practice-6-6-6",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n先固定可比较的实现版本：带提前结束的起泡、直接插入、简单选择、中间元素枢轴的快速、声明明确增量序列的希尔、最大堆堆排。所有排序从同一份基准数组副本开始，复制基准数组不计入指标；所有关键字比较只能经 Less(x,y)，每调用一次比较数加一；所有关键字赋值经 Move(dst,src)，移动数加一；交换必须由三次 Move 构成，固定计三次移动。临时关键字的保存和写回必须统一，本实现计入，避免低估插入排序。\n\n~~~~text\nfor each dataset D:\n  base=D\n  for each of six algorithms A:\n    work=copy(base); counters=(0,0); A(work,counters)\n    assert work nondecreasing and Multiset(work)==Multiset(base)\n    record dataset-name, algorithm, comparisons, moves\n~~~~\n\n表长取不少于 100，至少测试正序、逆序、两个固定种子的随机排列和一个近乎有序排列；可加高重复关键字组。记录 n、随机数发生器、种子、枢轴规则、希尔增量和编译环境，才能复现。通常选择排序比较数恒为 n(n-1)/2、移动较少；起泡和插入在正序或近序接近 O(n)，在逆序或乱序趋于 O(n^2)；快速平均 O(n log n) 但最坏 O(n^2)；希尔取决于增量；堆排 O(n log n) 且对初始次序较不敏感。各算法工作数组 O(1)，快速排序递归栈平均 O(log n)、最坏 O(n)。\n\n排序性与排列守卫分别发现顺序错误和元素丢失或重复；统一包装比较、移动使统计和算法动作一一对应。六种算法对 g 组长度 n 数据的总时间为各自运行时间之和，基准和工作数组空间 O(n)。",
        "explanation": "提供统一计数口径、可复现实验组、排序与排列验证，以及六算法的复杂度解释。",
        "correction": {
          "verified": "## 6.6 六种内部排序的比较程序\n\n**实验约定。** 输入 `n≥100` 和固定种子；从同一份基准数组复制出工作数组，依次运行带提前结束的起泡、直接插入、简单选择、中轴为中间元素的快速、Shell（增量 `n/2,n/4,...,1`）和最大堆排序。复制基准数组不计入指标。所有关键字比较只能经 `Less(a,b)`，每调用一次 `cmp++`；所有写入只能经 `Move(dst,src)`，每调用一次 `mov++`，交换必须由三次 Move 构成。\n\n```text\nLess(a,b,C): C.cmp++; return a<b\nMove(dst,src,C): dst=src; C.mov++\nSwap(a,b,C): t=a; Move(a,b,C); Move(b,t,C)\n\nBubble(a): for end=n-1..1：changed=false；for j=0..end-1：\n             若 Less(a[j+1],a[j])：Swap；changed=true；若 !changed 则结束\nInsertion(a): for i=1..n-1：x=a[i]；C.mov++；j=i-1；\n              while j>=0 且 Less(x,a[j])：Move(a[j+1],a[j])；j--；Move(a[j+1],x)\nSelection(a): for i=0..n-2：m=i；for j=i+1..n-1：若 Less(a[j],a[m]) m=j；若 m!=i Swap\nQuick(a,l,r): i=l,j=r,p=a[(l+r)/2]；循环推进 i、j 并在 i<=j 时 Swap；递归两侧\nShell(a): 对 gap=n/2;gap>0;gap/=2，执行以 gap 为间隔的 Insertion\nHeap(a): BuildMaxHeap；for end=n-1..1：Swap(a[0],a[end])；SiftDown(0,end)\n\nRun(dataset,algorithm): work=copy(dataset)；C=(0,0)；algorithm(work,C)；\n  断言 work 非递减且 work 与 dataset 的多重集合相同；输出算法名、cmp、mov\n```\n\n至少生成五组长度相同的数据：正序、逆序、两个确定种子的随机排列、近乎有序（从正序作固定次数交换）；可附加大量重复键。输出表的列为 `dataset, n, seed, algorithm, comparisons, moves`，并记录编译环境、中轴规则、Shell 增量。\n\n**可复验样例。** 对 `[3,1,2]`，选择排序比较数固定为 3，第一次交换 `[3,1]` 计 3 次移动；插入排序会得到 `[1,2,3]`。对 `n=100` 正序数据，带提前结束的起泡排序只进行 99 次比较、0 次交换（若临时取出/写回计入移动则按实现记录），而简单选择排序仍为 `100×99/2=4950` 次比较。\n\n应解释：起泡和插入对正序、近序敏感，逆序趋于 `O(n^2)`；选择的比较数基本固定；快速平均 `O(n log n)`、最坏 `O(n^2)`；Shell 取决于增量；堆排序 `O(n log n)` 且对初始次序不敏感。除快速递归栈（平均 `O(log n)`、最坏 `O(n)`）外，算法工作空间 `O(1)`；基准和工作数组另需 `O(n)`。",
          "explanation": "保留统一比较/移动计数、可复现实验集和排序/排列双重断言，补全了六个算法的可执行伪代码、计数口径、样例和结果解释。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入内部排序比较实验的独立、可复现方案。"
      }
    },
    {
      "id": "book-ds-yan-practice-6-6-7",
      "answer": {
        "status": "provided",
        "origin": "verified",
        "verified": "**独立参考解答（非原书摘录）**\n\n设每条记录有 q（不超过 5）个 0 到 100 的关键字，用户给出从最高到最低优先级的无重复字段序列 p[0..r-1]；默认字段升序，若支持降序则把每一优先级方向一并记录。LSD 的正确顺序是从 p[r-1] 到 p[0] 依次作稳定排序，不能从最高优先级先排。\n\n策略一对当前全表按一个字段执行稳定归并排序：合并时键相等先取左半，保持原有次序。策略二对同一字段准备 101 个队列 head[0..100]、tail[0..100]，按当前顺序把记录分配到对应队尾，再从 0 到 100 收集；每轮重置队头尾。两种策略的外层均为：从 i=r-1 递减至 0，对 records 按 p[i] 进行稳定排序或分配收集。输入 n 不得超过 1000，字段值越界、优先级重复或字段号非法均拒绝。\n\n归纳地，在处理第 i 字段前，序列已按较低优先级后缀有序；对第 i 字段的稳定排序不破坏同值记录的后缀顺序，因此最终按完整优先级序列字典序有序。对 5 到 8 组同一随机记录测试两策略，计时包含每轮排序或分配收集、排除生成和打印，并取多次重复的中位数；再用直接字典序比较器校验两输出完全相同。稳定归并策略 O(r*n log n) 时间、O(n) 辅助空间；分配收集 O(r*(n+101)) 时间、O(n+101) 空间，在键域固定时为 O(r*n)。",
        "explanation": "明确 LSD 处理方向、两种稳定策略、101 桶、输入边界和可比较实验。",
        "correction": {
          "verified": "## 6.7 LSD 多关键字排序\n\n每条记录有不超过 5 个整数关键字 `key[0..q-1]`，每个值在 `0..100`，记录数 `n≤1000`。用户给出无重复字段序列 `p[0..r-1]`，其顺序从**最高**到最低优先级；本题默认升序。输入时检查 `1≤r≤q`、字段号合法且不重复、所有键在值域内。LSD 的正确处理顺序是从最低优先级 `p[r-1]` 到最高优先级 `p[0]`，每一轮必须稳定。\n\n```text\nStableMergeSort(A, field):\n  递归排序左右半区；合并时若 key[left][field] <= key[right][field]，先取左项\n\nByStableSort(A,p,r):\n  for i=r-1 downto 0：StableMergeSort(A,p[i])\n\nByBuckets(A,p,r):\n  for i=r-1 downto 0：\n      head[0..100]=tail[0..100]=空\n      按 A 当前次序遍历每条记录 x：v=x.key[p[i]]；把 x 追加到第 v 个队尾\n      按 v=0..100 依次连接各非空队列，得到新的 A\n```\n\n归纳证明：处理字段 `p[i]` 前，序列已经按较低优先级后缀 `p[i+1..r-1]` 有序；对 `p[i]` 的稳定排序把记录按该字段分组，而同值组保持原后缀顺序，所以处理结束后按 `p[i..r-1]` 字典序有序。循环结束即按用户给定完整优先级有序。分配-收集按当前次序入同一桶的队尾，因此同样稳定。\n\n**可复验样例。** 三字段记录 `(id;总分,数学,英语)` 为 `A;(90,80,70)`、`B;(90,80,60)`、`C;(95,50,50)`、`D;(90,90,40)`，用户优先级为 `[总分,数学,英语]`。LSD 依次按英语、数学、总分稳定处理，升序输出应为 `B,A,D,C`：90 分组内数学 80 在 90 前，数学同为 80 的 B/A 再由英语 60/70 决定。两种策略必须输出完全相同的 id 序列。非法优先级 `[0,0]` 或键值 101 必须在排序前拒绝。\n\n对 5 至 8 组同规模、同种子随机记录分别计时；计时只覆盖排序（或分配收集），不含生成和打印，并多次运行取中位数。稳定归并策略时间 `O(r*n log n)`、辅助空间 `O(n)`；101 桶分配收集时间 `O(r*(n+101))`、空间 `O(n+101)`，本题固定值域时为 `O(r*n)`。",
          "explanation": "保留 LSD 从低优先级到高优先级、稳定内部排序与 101 桶分配收集两条主线，补足输入约束、稳定性证明、可复验样例及比较口径。",
          "reason": "全量答案复核：将方案提示重写为可落地完整答案。"
        }
      },
      "review": {
        "status": "passed",
        "resolvedFlagCodes": [],
        "notes": "已补入多关键字 LSD 排序的两种策略及综合比较方案。"
      }
    }
  ],
  "unresolved": []
}
